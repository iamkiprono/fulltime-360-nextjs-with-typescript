import React from "react";
import AccountComponent from "./AccountComponent";

const Account = async () => {
  return (
    <div>
      <AccountComponent />
    </div>
  );
};

export default Account;
